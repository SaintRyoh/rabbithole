return setmetatable({}, {
    __constructor = function(
        statusbar, 
        layouts, 
        globalKeybindings, 
        clientKeybindings, 
        mainmenu, 
        globalMouseButtons, 
        rules, 
        titlebar
    )
        return 
    end,
})