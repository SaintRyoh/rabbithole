return setmetatable({}, {
    __constructor = function(
        statusbar, 
        layouts, 
        globalKeybindings, 
        clientKeybindings, 
        mainmenu, 
        globalMouseButtons, 
        rules, 
        titlebar,
        main__sloppy___focus
    )
        return 
    end,
})