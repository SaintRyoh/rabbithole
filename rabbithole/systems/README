Systems are made out of:
    settings (injected settings)
    components (buttons,keys,rules,layouts)

What should Systems do? 
    * register signal handlers
    * or perform one-off operations


should only be injected at the environment level